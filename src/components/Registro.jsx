function RegistroActividad (fecha){
    let dia = fecha.toLocaleDateString();
    let hora = fecha.toLocaleTimeString();
    let cambios = [];
    let cambio = "Se registro un cambio a las " + hora + " del dia " + dia;
    const agregarCambios = (nuevo) => {
    };

    return (
        <div>

        </div>
    );
};